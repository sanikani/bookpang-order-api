# Transaction, Retry, Compensation, Lock Strategy Notes

이 문서는 현재 저장소와 이번 대화에서 논의한 내용을 바탕으로 다음 주제를 정리한 문서다.

- `@Retryable`과 `@Transactional`이 같은 흐름에 있을 때 발생하는 구조적 문제
- 주문 전체 재시도 구조와 그 한계
- 재고 처리만 별도 트랜잭션으로 분리하고 보상 트랜잭션을 적용한 구조
- 낙관적 락과 비관적 락의 차이, 그리고 도서 특성별 혼합 적용 전략

문서의 목적은 단순 구현 요약이 아니라, 왜 이런 구조 문제가 발생했는지와 어떤 설계 판단이 필요한지를 정리하는 것이다.

---

## 1. `@Retryable`과 `@Transactional`을 함께 사용할 때의 핵심 문제

### 1.1 기존 구조

현재 주문 저장의 기본 흐름은 다음과 같았다.

- `OrderProcessService.processOrder()`가 주문 전체를 하나의 트랜잭션으로 처리
- 그 안에서 `StockService.decreaseStock()`를 호출
- `BookStock`은 `@Version`으로 낙관적 락 적용
- `StockService.decreaseStock()`에는 `@Retryable` 적용

즉 표면적으로는 "재고 충돌이 나면 재고 차감만 재시도"하는 것처럼 보였지만, 실제 동작은 더 복잡하다.

### 1.2 왜 문제가 되는가

핵심 문제는 **재고 차감이 별도 트랜잭션이 아니라 주문 전체 트랜잭션 안에 포함되어 있었다는 점**이다.

이 경우 다음 문제가 발생한다.

1. `StockService.decreaseStock()`가 재고 엔티티의 상태만 변경하고 끝나면, 실제 SQL update는 JPA flush/commit 시점에 실행될 수 있다.
2. 낙관적 락 예외는 메서드 내부가 아니라 바깥 트랜잭션 commit 시점에 터질 수 있다.
3. 그러면 `@Retryable`이 감싼 메서드 바깥에서 예외가 발생하므로, 재고 서비스 메서드 재시도가 기대한 형태로 동작하지 않는다.
4. 결과적으로 재고 충돌 하나 때문에 주문 전체 트랜잭션이 롤백되고, 주문 전체를 다시 수행해야 하는 구조가 된다.

### 1.3 구조적으로 정리하면

기존 구조의 본질은 다음과 같다.

- 재시도 대상: 재고 처리만 의도
- 실제 롤백 범위: 주문 전체
- 예외 발생 시점: 재고 메서드 내부가 아니라 바깥 트랜잭션 flush/commit 가능성

즉, **"재고만 재시도"하고 싶었지만 실제로는 "주문 전체 재시도"가 되어버리는 구조**였다.

### 1.4 재현 테스트로 확인한 내용

이번 대화에서는 이 문제를 추상적으로만 논의하지 않고, 실제 주문 경로에서 재현 테스트로 확인했다.

- `OrderProcessServiceConcurrencyTest`
- 동일 ISBN에 대해 동시 주문을 넣는 구조
- 수정 전에는 `ObjectOptimisticLockingFailureException`이 주문 흐름에서 발생

이 테스트를 통해 다음을 확인했다.

- 단순히 `StockService` 단독 테스트가 아니라
- **실제 주문 트랜잭션 안에서**
- 낙관적 락 충돌이 재고 처리 메서드 단위가 아니라 주문 흐름 전체를 깨뜨린다는 점

---

## 2. 1차 구조 개선: 주문 전체 재시도 구조

### 2.1 개선 아이디어

가장 먼저 적용한 개선은 다음이었다.

- `OrderProcessService`는 retry wrapper 역할만 수행
- 실제 주문 DB 작업은 별도 `OrderProcessTransactionalService`로 분리
- `OrderProcessTransactionalService.processOrder()`에 `@Transactional`
- `OrderProcessService.processOrder()`에 `@Retryable`

즉, 재시도 단위를 재고 서비스가 아니라 **주문 전체 트랜잭션 메서드**로 끌어올렸다.

### 2.2 이 구조에서 좋아진 점

이 방식으로 바꾸면 적어도 다음은 해결된다.

- 낙관적 락 예외가 주문 트랜잭션 내부에서 발생해도
- 재시도 프록시가 주문 전체 호출을 다시 감쌀 수 있다
- 즉, 구조적으로는 실제 동작과 재시도 범위가 일치한다

또한 주문 만료 메시지 전송은 `AFTER_COMMIT` 이벤트 리스너로 옮겨,

- 주문이 롤백되었는데 메시지가 먼저 나가는 문제
- 트랜잭션과 비동기 후속 작업의 경계가 뒤섞이는 문제

를 줄였다.

### 2.3 하지만 왜 이 구조만으로는 부족한가

이 방식은 안전하지만 비용이 크다.

재고 충돌 하나가 나더라도 다음 작업이 모두 다시 수행될 수 있다.

- 주문 상품 생성
- 가격 계산
- 할인 처리
- 주문 저장
- 주문 상품 저장
- 포인트 후속 처리 준비

즉, 동시성 충돌이 많은 상품에서는 **재고 충돌 비용이 주문 전체 재수행 비용으로 확장**된다.

### 2.4 언제 이 구조가 괜찮은가

이 구조는 다음 상황에서는 나쁘지 않다.

- 충돌 빈도가 낮음
- 주문 전체 원자성을 우선시함
- 설계를 단순하게 유지하고 싶음
- hot item이 거의 없음

하지만 베스트셀러, 한정판, 타임세일처럼 특정 ISBN에 주문이 몰리는 경우에는 비효율이 커질 수 있다.

---

## 3. 2차 구조 개선: 재고만 별도 트랜잭션으로 분리 + 보상 트랜잭션

### 3.1 왜 이 구조가 필요했는가

논의의 핵심은 다음이었다.

- 주문 전체 재시도는 비용이 크다
- 재고 충돌은 재고 row 하나의 hot spot 문제다
- 그렇다면 재시도 범위를 재고 처리 구간으로 줄이는 게 더 합리적일 수 있다

하지만 여기서 중요한 제약이 있다.

**재고만 별도 트랜잭션으로 분리하면 주문 전체 ACID 원자성은 약해진다.**

즉, 재고 예약은 성공했는데 주문 저장이 실패할 수 있다.  
이 경우 재고를 다시 되돌리는 **보상 로직**이 필요하다.

### 3.2 새로 추가한 구조

이번 대화에서 비교용으로 추가한 새 경로는 다음과 같다.

#### 1) `CompensatingOrderProcessService`

- 바깥 orchestration 전용
- 주문 전체를 하나의 큰 트랜잭션으로 묶지 않음
- 순서는 다음과 같다.
  1. 재고 예약
  2. 주문 확정 처리
  3. 실패 시 재고 복구 보상

#### 2) `StockReservationService`

- retry wrapper 역할
- `reserve()` / `release()`에 `@Retryable`
- 낙관적 락 충돌이 발생하면 재고 처리 구간만 재시도

#### 3) `StockReservationTransactionalService`

- 실제 재고 예약/복구 처리 담당
- `@Transactional(propagation = REQUIRES_NEW)`
- `bookStockRepository.flush()` 호출

여기서 `REQUIRES_NEW`와 `flush()`가 중요한 이유는 다음과 같다.

- `REQUIRES_NEW`: 주문 후반부와 분리된 독립 트랜잭션으로 실행
- `flush()`: 낙관적 락 충돌을 메서드 내부에서 즉시 확정

즉, 예외가 메서드 바깥 commit 시점으로 밀리지 않고 재고 처리 메서드 안에서 발생해 retry가 의미를 갖는다.

#### 4) `CompensatingOrderFinalizeService`

- 주문 확정 단계 담당
- `@Transactional`
- 주문상품 생성
- 가격 계산
- 할인 처리
- 주문 저장
- 포인트 적립 후속 처리 준비
- 커밋 후 주문 메시지 발행

### 3.3 흐름 요약

새 구조의 전체 흐름은 다음과 같다.

1. `CompensatingOrderProcessService`
2. `StockReservationService.reserve(items)`
3. `StockReservationTransactionalService.reserve(items)`  
   `REQUIRES_NEW + flush`
4. 재고 예약 성공 후 `CompensatingOrderFinalizeService.finalizeOrder(command)`
5. 주문 후반 처리 실패 시 `StockReservationService.release(items)` 실행
6. `StockReservationTransactionalService.release(items)`로 재고 복구

### 3.4 이 구조의 장점

- 재고 충돌이 나도 주문 전체를 다시 수행하지 않고 재고 처리 구간만 재시도 가능
- hot row 충돌 비용을 줄일 수 있음
- 실패 시 보상 로직으로 재고 정합성 복구 가능
- 기존 구현을 유지한 채 비교 가능한 별도 경로를 만들 수 있음

### 3.5 이 구조의 한계

이 구조는 장점만 있는 게 아니다.

1. 주문 전체 원자성이 약해진다.
2. 보상 로직이 누락되면 정합성 문제가 생긴다.
3. 앞으로 주문 후반부에 외부 부작용이 더 추가되면, 재고 외 자원도 같이 보상 설계가 필요하다.

즉, 이건 "문제를 없애는 구조"라기보다  
**"원자성 일부를 포기하고 충돌 비용을 줄이는 구조"**라고 보는 것이 정확하다.

### 3.6 테스트로 검증한 내용

새 구조는 다음 테스트로 검증했다.

- `CompensatingOrderProcessServiceConcurrencyTest`
  - 동일 ISBN 동시 주문 시 재고 처리만 재시도되어 주문이 정상 완료되는지 확인
- `CompensatingOrderProcessServiceCompensationTest`
  - 주문 후반 처리 실패 시 선점한 재고가 원복되는지 확인

이 테스트를 통해 단순 설계 설명이 아니라,

- 재고 충돌 시 부분 재시도 가능
- 주문 후반 실패 시 보상 복구 동작

을 코드 레벨에서 증명했다.

---

## 4. 낙관적 락과 비관적 락의 차이

### 4.1 낙관적 락

낙관적 락은 "충돌이 드물 것"이라는 가정 아래 동작한다.

장점:

- 평소 락 대기 비용이 거의 없다
- 일반 상품처럼 충돌이 적은 row에서는 효율적이다
- read-heavy / low-contention 상황에 적합하다

단점:

- 충돌이 많아질수록 재시도 비용이 커진다
- hot row에서는 성능이 급격히 나빠질 수 있다
- 결국 CPU, DB round trip, retry overhead가 커진다

### 4.2 비관적 락

비관적 락은 "충돌 가능성이 크다"는 전제로 row를 먼저 점유한다.

장점:

- 충돌 상황에서 재시도보다 예측 가능한 직렬화가 가능하다
- oversell 방지처럼 강한 정합성이 필요한 hot item에 유리하다

단점:

- 락 대기 시간이 생긴다
- 평소에도 병목이 생길 수 있다
- 일반 상품까지 모두 비관적 락으로 처리하면 오히려 전체 성능이 떨어질 수 있다

### 4.3 도서 재고에 단일 전략만 쓰는 것이 왜 위험한가

모든 도서를 낙관적 락 하나로 처리하는 구조는 단순하지만, 상품 특성을 반영하지 못한다.

예를 들면:

- 일반 도서: 충돌 적음 → 낙관적 락 적합
- 베스트셀러: 충돌 높음 → 낙관적 락 재시도 비용 증가
- 한정판 / 특가 상품: 충돌 매우 높음 → 비관적 락 또는 별도 직렬화가 더 적합

즉, **모든 도서에 동일한 락 전략을 강제하는 것은 현실적인 최적해가 아니다.**

---

## 5. 도서 특성에 따라 락 전략을 다르게 적용하는 방법

### 5.1 추천 방향: 혼합 전략

가장 현실적인 방향은 다음과 같다.

- 기본 전략: 낙관적 락
- 특정 hot item: 비관적 락 또는 별도 직렬화

즉, 상품 특성에 따라 락 전략을 선택할 수 있는 구조가 필요하다.

### 5.2 추천 설계

예를 들면 다음과 같이 분리할 수 있다.

```java
public interface StockLockStrategy {
    void decrease(String isbn, int quantity);
    void increase(String isbn, int quantity);
}
```

구현체:

- `OptimisticStockLockStrategy`
- `PessimisticStockLockStrategy`

선택기:

- `StockLockStrategyResolver`

이 resolver는 ISBN 또는 상품 속성에 따라 전략을 고른다.

### 5.3 초기 적용 방법

자동 판단보다 먼저 하기 쉬운 방식은 수동 정책 지정이다.

예시:

- `BookStock` 또는 별도 설정 테이블에 `lockPolicy` 저장
- 값: `OPTIMISTIC`, `PESSIMISTIC`
- 일반 도서는 `OPTIMISTIC`
- 베스트셀러/한정판은 운영자가 `PESSIMISTIC`으로 전환

이 방식의 장점은 다음과 같다.

- 구현 단순
- 운영 제어 가능
- 성능 측정 후 점진적 확장 가능

### 5.4 이후 확장 방향

더 발전시키려면 아래 기준으로 자동 전환까지 갈 수 있다.

- 최근 충돌 횟수
- 최근 재시도 실패율
- 최근 주문량
- 상품 등급
- 한정판 여부

다만 포트폴리오나 실습 단계에서는 수동 전환 구조만 있어도 충분히 좋은 설계 포인트가 된다.

### 5.5 더 나아간 대안

hot item이 극단적으로 많아지면 DB 락 전략만으로도 병목이 생길 수 있다.

이 경우 고려할 수 있는 대안:

- Redis 분산락
- 재고 예약 큐 기반 직렬화
- 별도 재고 예약 테이블

즉, 비관적 락은 hot item 대응의 한 방법일 뿐, 항상 최종 해답은 아니다.

---

## 6. 이번 대화에서 정리된 설계 판단

이번 논의의 핵심 판단은 다음과 같다.

### 판단 1

`@Retryable`과 `@Transactional`을 단순히 같이 붙이는 것만으로는 충분하지 않다.

중요한 것은

- 예외가 어디서 발생하는가
- 어떤 트랜잭션 범위가 롤백되는가
- 재시도 범위가 실제로 어디인가

이다.

### 판단 2

재고 충돌이 적다면 주문 전체 재시도도 현실적인 선택이 될 수 있다.

하지만 hot item이 존재한다면

- 주문 전체 재시도 비용
- 재고 row 충돌 빈도

를 고려해 재고 처리만 별도 트랜잭션으로 분리하는 구조가 더 적절할 수 있다.

### 판단 3

재고만 별도 트랜잭션으로 분리하면 보상 로직이 반드시 필요하다.

즉, 이 구조는 단순한 retry 최적화가 아니라,

- 트랜잭션 경계 재설계
- 부분 실패 허용
- 보상 복구 설계

가 함께 따라오는 구조다.

### 판단 4

모든 상품에 단일 락 전략을 강제하는 것은 적절하지 않다.

도서 도메인에서는

- 일반 상품
- 베스트셀러
- 한정판

의 충돌 특성이 다르기 때문에, 장기적으로는 혼합 전략이 더 맞다.

---

## 7. 성능 개선을 실제로 수치화하려면 무엇을 재야 하는가

이번 대화에서 추가로 논의한 성능 측정 포인트는 다음과 같다.

### 7.1 기존 구조 vs 새 구조 비교

비교 대상:

- 주문 전체 재시도 구조
- 재고만 재시도 + 보상 복구 구조

측정 지표:

- 평균 응답 시간
- p95 / p99 지연 시간
- 재시도 횟수
- 성공률
- 총 SQL 실행 수

### 7.2 충돌 부하 실험

같은 ISBN에 대해 동시 주문을 2, 5, 10, 20, 50 등으로 늘려가며 비교할 수 있다.

이렇게 하면 다음 질문에 답할 수 있다.

- 충돌이 적을 때는 두 구조 차이가 얼마나 작은가
- 충돌이 커질수록 주문 전체 재시도 구조가 얼마나 비싸지는가
- 새 구조가 충돌 구간만 재시도하면서 얼마나 비용을 줄이는가

### 7.3 주의점

- H2보다 MySQL 기준 측정이 더 의미 있다
- warm-up 없이 첫 실행 수치만 보면 왜곡될 수 있다
- 평균값만 말하지 말고 p95 / p99를 같이 봐야 한다
- 측정 전에는 "성능 향상" 문구를 과장해서 쓰면 안 된다

---

## 8. 이 문서의 결론

이번 논의에서 핵심적으로 정리된 내용은 다음과 같다.

1. 기존 구조는 재고만 재시도하려는 의도와 달리, 실제로는 주문 전체가 재시도되는 방향으로 흘러갈 수 있었다.
2. 이를 해결하는 첫 번째 방법은 주문 전체 재시도 구조로 재시도 범위와 실제 동작을 맞추는 것이다.
3. 하지만 충돌 비용을 줄이려면 재고를 별도 트랜잭션으로 분리하고, 주문 실패 시 보상 복구를 수행하는 구조가 더 적절할 수 있다.
4. 다만 이 방식은 전체 원자성을 일부 포기하는 대신 보상 트랜잭션으로 정합성을 맞추는 구조라는 점을 분명히 이해해야 한다.
5. 장기적으로는 모든 도서에 동일 락 전략을 적용하기보다, 상품 특성에 따라 낙관적 락과 비관적 락을 혼합 적용하는 방향이 더 현실적이다.

즉, 이 문제는 단순히 "retry를 붙일까 말까"의 문제가 아니라,

- 트랜잭션 경계
- retry 범위
- 예외 발생 시점
- 보상 트랜잭션
- 상품 특성별 락 전략

을 함께 고려해야 하는 백엔드 설계 문제라고 볼 수 있다.
